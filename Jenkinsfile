pipeline {
    agent any

    stages {
       stage ("Build Stage") {
         steps {
        checkout scm 
        echo "run the script"
        sh "ls -lrt"
        /* .. snip .. */
     }
   }
 }
}
